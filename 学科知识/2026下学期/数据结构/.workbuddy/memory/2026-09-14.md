# 2026-09-14

## 通读第2章线性表 PPT（74 页）并整理课程结构

- 用 `pdftotext -layout` 提取 `第2章 线性表.pdf` 全文（74 页；PDF 元数据里的
  Title 误写成"第1章 绪论"，是复制文件时没改，不要被误导）
- 同法提取 `课程说明.pdf`：14 周 / 56 学时、评分标准、不讲不考清单
- 结论已写入 `MEMORY.md`（课程结构 + 第 2 章四节知识树）
- 输出形式：两张内联可视化（第 2 章知识结构树、各章学时条形图）
