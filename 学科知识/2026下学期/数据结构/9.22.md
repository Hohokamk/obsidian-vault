if (pos<1 || pos>S.length || len<0 || len>S.length-pos+1)
return ERROR;



![[Pasted image 20260922081631.png|439]]

模式匹配：
![[Pasted image 20260922082435.png|439]]
信息来自两个地方：Si 之前的全部匹配上
				 ti 和 ti-1 是否相等
事先要充分分析串 t,先验很多信息。模式串的特性很重要，了解 t 就可以知道排除没有意义的操作，需要有


信息来源：模式串自身的特性和主串子串匹配的结果
将 j 失配时下一次 j 的取值记为 next[i]=k;

next[]数组怎么求
前 j-1 个字符的子串
next[0]=-1
next[1]=0

双向链表的神奇之处：
![[Pasted image 20260923223948.png]]