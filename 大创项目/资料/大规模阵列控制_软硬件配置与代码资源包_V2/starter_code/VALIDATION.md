# 验证说明

## 已完成的离线验证

- Python源代码通过 `compileall` 语法检查；
- `pytest` 回归测试共11项通过：
  - CRC16标准向量；
  - 协议编码/解析；
  - CRC错误拒绝；
  - 串口字节流分片解析；
  - 256路配置与32字节帧；
  - 单点、棋盘格和随机图案pack/unpack；
  - 单单元阵列因子；
  - 连续相位20°指向；
  - 1 bit权重取值检查；
- 映射生成脚本已运行，能生成CSV和C头文件；
- `arrayctl.py --dry-run` 已验证256路单点产生32字节PAYLOAD；
- 阵列仿真演示已运行，连续相位主瓣达到设定25°；
- 仿真同时暴露并记录了理想 `+1/-1` 实权重可能产生正负对称双波束的问题；
- 二值随机搜索示例已运行。

## 尚未在本环境验证的部分

- 当前容器未安装 `pyserial`，真实串口访问需按 `requirements.txt` 安装；`--dry-run`不依赖串口；
- STM32代码是需要接入CubeMX工程的HAL骨架，未在本环境交叉编译；
- 具体SPI结束标志必须按所选STM32G4、CubeG4版本和RM0440确认；
- KiCad/KiBot脚本需在安装KiCad 10和对应KiBot版本的电脑上验证；
- 驱动器引脚、OE/CLR极性和额定值必须按实际采购型号的数据手册复核；
- 所有电气指标必须由学生在8/16/64/256路实物上测量，不能以代码测试替代。
