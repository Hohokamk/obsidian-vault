1. 云台控制 ：
```mermaid
graph TD
    A["IMU 测量底盘当前朝向<br/>GetAngleYaw() / GetAnglePitch()"] --> B["计算误差 delta<br/>= 目标角度 - 当前角度"]
    B --> C["角度回绕处理<br/>delta 限制在 ±4096 内"]
    C --> D{"|delta| >= 10?"}
    D -->|否| E["不动（死区）"]
    D -->|是| F["增量PID 输出修正量"]
    F --> G["mark_yaw / mark_pitch += 修正量"]
    G --> H["PANTILE::Update()<br/>把 mark 赋给 setangle"]
    H --> I["电机 POS 模式<br/>自动转到目标角度"]
```


达妙电机
gitee 产品资料
电机分类
can 线，终端电阻
can 分析仪
正线序和反线序
dm 调试工具功能：校准
MIT 模式：1. 关节动力学模型 ![[Pasted image 20260804103059.png]] 期望的力矩：位置误差和速度误差
发送量：刚性，阻尼，力矩补偿，期望位置，期望速度
与 pid 的区别：不是刚硬，而是柔顺
![[Pasted image 20260804103502.png]] 电机参数设置：
1. 控制幅值
2. 驱动参数
3. 通讯设置
4. 控制设置
![[Pasted image 20260804110335.png]]

![[Pasted image 20260804110400.png]]
```mermaid
graph TD
    subgraph "ControlTask（5ms周期）"
        A["rc.Update()<br/>遥控器解码"] --> B["Control_Pantile()<br/>摇杆 → mark_yaw / mark_pitch"]
        B --> C["ctrl.pantile.Update()"]
        C --> D["DMmotor[0].setPos = mark_yaw<br/>DMmotor[1].setPos = mark_pitch"]
    end

    subgraph "MotorUpdateTask（2ms周期）"
        D --> E["DMmotor[i].DMmotor_Ontimer()<br/>setPos → CAN数据帧<br/>存入 can2.jointpdata[i]"]
    end

    subgraph "CanTransimtTask（1ms周期）"
        E --> F["DMmotor[i].DMmotor_transmit()<br/>发送 can2.jointpdata[i]<br/>CAN2 ID=0x101/0x102"]
    end

    F --> G["达妙电机转动"]
```
![[Pasted image 20260804112449.png]]![[屏幕截图 2026-08-04 112401.png]]