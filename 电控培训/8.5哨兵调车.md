![[Pasted image 20260804173151.png]] float delta = 0, adjust = sensitivity;
if (type == YAW)
{
	delta = degreeToMechanical(ctrl.GetDelta(angleKeep - frameOfReference.GetAngleYaw()));
	if (delta <= -4096.f)
		delta += 8192.f;
	else if (delta >= 4096.f)
		delta -= 8192.f;
	if (abs(delta) >= 10.f)
		mark_yaw += pantile_PID[PANTILE::YAW].Delta(delta);
}
else if (type==PITCH)
{
	delta = degreeToMechanical(ctrl.GetDelta(angleKeep - frameOfReference.GetAnglePitch()));
	if (delta <= -4096.f)
		delta += 8192.f;
	else if (delta >= 4096.f)
		delta -= 8192.f;

	if (abs(delta) >= 10.f)
	{
		mark_pitch += pantile_PID[PANTILE::PITCH].Delta(delta);
	}
}



keep_pantile
// 达妙电机位置范围：-4π ~ 4π（约 -12.57 ~ 12.57 弧度）
// yaw 可以无限旋转，但限制在范围内防止累积误差过大
if (mark_yaw > 4.0 f * PI) mark_yaw -= 8.0 f * PI;
if (mark_yaw < -4.0 f * PI) mark_yaw += 8.0 f * PI;

// pitch 限幅（假设机械限位 -45° ~ 45°，即 -0.785 ~ 0.785 弧度）
mark_pitch = std::max(std::min(mark_pitch, 0.785 f), -0.785 f);

// 赋给达妙电机（弧度）
DMmotor[0].setPos = mark_yaw;
// 如果有第二个达妙电机控制 pitch，取消下面的注释
DMmotor[1].setPos = mark_pitch;






遥控器设置
ch_pitch *= (-1.f);
ch_yaw *= (1.f);//方向相反修改这里正负
	
// 达妙电机用弧度，灵敏度调整
// ch 范围 -660~660，sensitivity=2.5，调整后每次累加约 0.005 弧度
float adjangle_rad = this->pantile.sensitivity * 0.002 f;

ctrl.pantile.mark_pitch -= (float)(adjangle_rad * ch_pitch);
ctrl.pantile.mark_yaw -= (float)(adjangle_rad * ch_yaw);


![[Pasted image 20260804175739.png]]

回调函数
if (hcan == &can 1.hcan)
	memcpy(can 1.data[hcan->pRxMsg->StdId - 0 x 201], hcan->pRxMsg->Data, sizeof(uint 8_t) * 8);
else
{
	if (hcan->pRxMsg->StdId >= 1 && hcan->pRxMsg->StdId <= 2)
	{
		memcpy(can 2.jointidata[hcan->pRxMsg->StdId - 1], hcan->pRxMsg->Data, sizeof(uint 8_t) * 8);
	}
	else
	{
		memcpy(can 2.data[hcan->pRxMsg->StdId - 0 x 201], hcan->pRxMsg->Data, sizeof(uint 8_t) * 8);
	}
}


全量式 Pid
position
speed
增量式 pid