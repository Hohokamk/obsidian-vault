互联网 + 产业赛道项目/
├── README.md                          # 项目说明文档
├── .gitignore
├── docker-compose.yml                 # Docker 一键部署
│
├── backend/                           # 🐍 后端 (Python FastAPI)
│   ├── requirements.txt
│   ├── Dockerfile
│   ├── .env                           # 环境配置
│   └── app/
│       ├── main.py                    # 应用入口 + 生命周期管理
│       ├── core/
│       │   ├── config.py              # 全局配置（MQTT/DB/告警阈值）
│       │   └── database.py            # 数据库引擎 + 会话管理
│       ├── models/
│       │   ├── device.py              # 4 张数据表（设备/数据/告警/汇总）
│       │   └── schemas.py             # Pydantic 请求响应模型
│       ├── api/
│       │   ├── dashboard.py           # 看板 API（总览/趋势/状态/告警）
│       │   └── mock.py                # 模拟数据生成
│       ├── mqtt/
│       │   └── client.py              # MQTT 客户端（订阅/解析/存储/告警）
│       └── services/
│
├── frontend/                          # 🖥️ 前端 (Vue 3 + ECharts)
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   ├── Dockerfile + nginx.conf
│   └── src/
│       ├── main.js                    # 入口
│       ├── App.vue                    # 主框架（导航 + 时间）
│       ├── router/index.js            # 3 个页面路由
│       ├── api/index.js               # 后端 API 封装
│       ├── stores/dashboard.js        # Pinia 状态管理
│       ├── styles/global.scss
│       └── views/
│           ├── Dashboard.vue          # ⭐ 主看板（统计卡片+折线图+饼图+告警表）
│           ├── DeviceList.vue         # 设备管理页
│           └── AlarmList.vue          # 告警中心页
│
├── mqtt-broker/
│   └── emqx.conf                      # EMQX Broker 配置
│
├── tools/
│   └── mqtt_simulator.py              # MQTT 数据模拟器（测试用）
│
└── docs/
    ├── 商业计划书.md                   # 完整 BP（市场/竞品/商业模式/财务）
    ├── 需求调研模板.md                 # 企业调研问卷
    └── 合作方案模板.md                 # 合作邀请函
![[Pasted image 20260822001231.png|455]]